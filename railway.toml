[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/bin/sh -c 'uv run uvicorn cellophanemail.app:app --host 0.0.0.0 --port ${PORT:-8000}'"
healthcheckPath = "/health/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
